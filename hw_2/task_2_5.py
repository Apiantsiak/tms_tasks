# Create a string equal to all elements of the input string with even numbers
# indices. (assuming that indexing starts at 0, so the characters
# are displayed starting from the first, indices 0,2,4,6 ....).

sentence = "Hello Python"
sentence_slice = sentence[0::2]

print(sentence_slice)
